#include "main.h"

/**
 *
 * main - check if a number from 1 to 9
 *
 * @_isdigit: return 1 if a number else 0
 *
 * Rrturn: always 0
 */
int _isdigit(inc c)
{
	if ((c >=0) && (c <= 9))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
